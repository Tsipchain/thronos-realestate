"""\nThronos Wallet V1 - Secure Signed Transaction Endpoints (Master/Replica Safe)\n\nArchitecture:\n- Master node: Accepts signed transactions, verifies signature, writes to SQLite\n- Replica node: Rejects with 503 (forwarding to master is future feature)\n- Both nodes: Use Redis for distributed nonce/replay protection\n"""\n\nfrom flask import request, jsonify\nfrom typing import Dict, Any, Tuple\n\n\ndef register_wallet_v1_endpoints(\n    app,\n    wallet_v1_module,\n    node_role: str,\n    read_only: bool,\n    master_sqlite_path: str,\n    send_thr_internal_fn,\n    transfer_custom_token_fn,\n    validate_address_fn\n):\n    """\n    Register wallet V1 signed transaction endpoints.\n\n    Args:\n        app: Flask application\n        wallet_v1_module: wallet_v1_production_final module\n        node_role: \"master\" or \"replica\"\n        read_only: True if node is read-only\n        master_sqlite_path: Path to master SQLite database\n        send_thr_internal_fn: Reference to send_thr_internal function\n        transfer_custom_token_fn: Reference to transfer_custom_token function\n        validate_address_fn: Reference to validate_thr_address function\n    """\n\n    @app.route(\"/api/v1/tx/send\", methods=[\"POST\"])\n    def api_v1_tx_send():\n        """\n        Secure transaction submission endpoint (v1).\n\n        Master behavior: Verify signature, check nonce (Redis), write to SQLite\n        Replica behavior: Return 503 with forwarding instructions (future feature)\n        """\n        # CRITICAL: Replica check FIRST\n        if read_only or node_role == \"replica\":\n            return jsonify({\n                \"ok\": False,\n                \"error\": \"read_only_replica\",\n                \"message\": \"This node is read-only. Submit transactions to the master node.\",\n                \"node_role\": node_role\n            }), 503\n\n        try:\n            data = request.get_json() or {}\n            signed_tx = data.get('tx')\n\n            if not signed_tx:\n                return jsonify({\n                    \"ok\": False,\n                    \"error\": \"missing_tx_envelope\"\n                }), 400\n\n            # Step 1: Verify Redis is available (fail-closed)\n            is_redis_ok, redis_error = wallet_v1_module.verify_redis_available()\n            if not is_redis_ok:\n                return jsonify({\n                    \"ok\": False,\n                    \"error\": redis_error,\n                    \"reason\": \"distributed_nonce_unavailable\"\n                }), 503\n\n            # Step 2: Verify master mode is available (fail-closed)\n            is_master_ok, master_error = wallet_v1_module.verify_master_mode_required(signed_tx)\n            if not is_master_ok:\n                return jsonify({\n                    \"ok\": False,\n                    \"error\": master_error,\n                    \"reason\": \"master_state_unavailable\"\n                }), 503\n\n            # Step 3: Core verification (works on any node)\n            is_valid, error_msg = wallet_v1_module.verify_signed_transaction_core(signed_tx)\n            if not is_valid:\n                return jsonify({\n                    \"ok\": False,\n                    \"error\": error_msg.split(':')[0],\n                    \"detail\": error_msg\n                }), 400 if 'validation_failed' in error_msg else 401\n\n            # Step 4: Extract and validate transaction details\n            from_addr = signed_tx.get('from')\n            to_addr = signed_tx.get('to')\n            amount = signed_tx.get('amount')\n            token = signed_tx.get('token', 'THR')\n            speed = signed_tx.get('speed', 'fast')\n\n            if not validate_address_fn(from_addr):\n                return jsonify({\n                    \"ok\": False,\n                    \"error\": \"invalid_from_address\"\n                }), 400\n\n            if not validate_address_fn(to_addr):\n                return jsonify({\n                    \"ok\": False,\n                    \"error\": \"invalid_to_address\"\n                }), 400\n\n            if amount <= 0:\n                return jsonify({\n                    \"ok\": False,\n                    \"error\": \"invalid_amount\"\n                }), 400\n\n            # Step 5: Process transaction on master\n            if token.upper() == 'THR':\n                result = send_thr_internal_fn(\n                    from_thr=from_addr,\n                    to_thr=to_addr,\n                    amount_raw=amount,\n                    auth_secret=\"\",  # Already authenticated via signature\n                    passphrase=\"\",\n                    speed=speed,\n                    tx_id=signed_tx.get('nonce')\n                )\n            else:\n                result = transfer_custom_token_fn(\n                    symbol=token,\n                    from_thr=from_addr,\n                    to_thr=to_addr,\n                    amount_raw=amount,\n                    auth_secret=\"\",\n                    passphrase=\"\",\n                    speed=speed\n                )\n\n            return result\n\n        except Exception as e:\n            return jsonify({\n                \"ok\": False,\n                \"error\": \"transaction_processing_failed\",\n                \"detail\": str(e)\n            }), 500\n\n    @app.route(\"/api/v1/tx/batch\", methods=[\"POST\"])\n    def api_v1_tx_batch():\n        \"\"\"\n        Batch transaction submission endpoint (v1).\n        Master only. Processes multiple signed transactions.\n        \"\"\"\n        if read_only or node_role == \"replica\":\n            return jsonify({\n                \"ok\": False,\n                \"error\": \"read_only_replica\",\n                \"message\": \"Batch transactions not supported on replica nodes.\"\n            }), 503\n\n        try:\n            data = request.get_json() or {}\n            transactions = data.get('transactions', [])\n\n            if not transactions:\n                return jsonify({\n                    \"ok\": False,\n                    \"error\": \"empty_transaction_list\"\n                }), 400\n\n            # Verify prerequisites (fail-closed)\n            is_redis_ok, _ = wallet_v1_module.verify_redis_available()\n            if not is_redis_ok:\n                return jsonify({\n                    \"ok\": False,\n                    \"error\": \"redis_unavailable\"\n                }), 503\n\n            results = []\n            for signed_tx in transactions:\n                # Core verification\n                is_valid, error_msg = wallet_v1_module.verify_signed_transaction_core(signed_tx)\n                if not is_valid:\n                    results.append({\n                        \"tx_id\": signed_tx.get('nonce'),\n                        \"ok\": False,\n                        \"error\": error_msg\n                    })\n                    continue\n\n                # Process\n                from_addr = signed_tx.get('from')\n                to_addr = signed_tx.get('to')\n                amount = signed_tx.get('amount')\n                token = signed_tx.get('token', 'THR')\n\n                try:\n                    if token.upper() == 'THR':\n                        result = send_thr_internal_fn(\n                            from_thr=from_addr,\n                            to_thr=to_addr,\n                            amount_raw=amount,\n                            auth_secret=\"\",\n                            passphrase=\"\",\n                            speed=signed_tx.get('speed', 'fast'),\n                            tx_id=signed_tx.get('nonce')\n                        )\n                    else:\n                        result = transfer_custom_token_fn(\n                            symbol=token,\n                            from_thr=from_addr,\n                            to_thr=to_addr,\n                            amount_raw=amount,\n                            auth_secret=\"\",\n                            passphrase=\"\",\n                            speed=signed_tx.get('speed', 'fast')\n                        )\n\n                    results.append({\n                        \"tx_id\": signed_tx.get('nonce'),\n                        \"ok\": True,\n                        \"result\": result\n                    })\n                except Exception as e:\n                    results.append({\n                        \"tx_id\": signed_tx.get('nonce'),\n                        \"ok\": False,\n                        \"error\": str(e)\n                    })\n\n            all_success = all(r.get('ok', False) for r in results)\n            return jsonify({\n                \"ok\": all_success,\n                \"results\": results\n            }), 200 if all_success else 207\n\n        except Exception as e:\n            return jsonify({\n                \"ok\": False,\n                \"error\": \"batch_processing_failed\",\n                \"detail\": str(e)\n            }), 400\n\n\ndef register_legacy_deprecation_endpoints(app):\n    \"\"\"\n    Register deprecation handlers for legacy endpoints.\n    Return 410 Gone (permanent removal).\n    \"\"\"\n\n    @app.route(\"/api/wallet/send\", methods=[\"POST\"])\n    def api_wallet_send_deprecated():\n        return jsonify({\n            \"ok\": False,\n            \"error\": \"legacy_endpoint_deprecated\",\n            \"message\": \"/api/wallet/send is deprecated. Use /api/v1/tx/send with signed envelopes.\",\n            \"upgrade_required\": True\n        }), 410\n\n    @app.route(\"/send_thr\", methods=[\"POST\"])\n    def send_thr_deprecated():\n        return jsonify({\n            \"ok\": False,\n            \"error\": \"legacy_endpoint_deprecated\",\n            \"message\": \"/send_thr is deprecated. Use /api/v1/tx/send with signed envelopes.\",\n            \"upgrade_required\": True\n        }), 410\n\n    @app.route(\"/api/tokens/transfer\", methods=[\"POST\"])\n    def api_tokens_transfer_deprecated():\n        return jsonify({\n            \"ok\": False,\n            \"error\": \"legacy_endpoint_deprecated\",\n            \"message\": \"/api/tokens/transfer is deprecated. Use /api/v1/tx/send with signed envelopes.\",\n            \"upgrade_required\": True\n        }), 410\n